export default function FriendModal({ handleClose }) {
    

    return(
        <div>
            <section className="modal-main">
                <button type="button" onClick={() => handleClose()}></button>
            </section>
        </div>
    );
}
